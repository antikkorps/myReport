# myReport — Backlog

Single source of truth pour suivre l'avancement. Mis à jour à chaque fin d'étape (avant commit). Rédigé en FR pour suivi perso, sans impact sur la règle "commentaires en anglais" du code.

**Légende**

- `[ ]` à faire
- `[~]` en cours
- `[x]` terminé (lint + typecheck + test verts + doc à jour + committé)
- `[!]` bloqué (voir note)

---

## Phase 0 — Setup

- [x] **Cadrage & ADR 0001** — doc d'architecture archivée + ADR des choix de stack. *(2026-04-23)*
- [x] **Scaffold monorepo** — pnpm workspaces, Turborepo, Biome 2, tsconfig strict, arborescence `apps/` + `packages/` + `docs/` + `infra/`. *(2026-04-23)*
- [x] **Infra locale** — `infra/docker-compose.yml` (Postgres 16 port hôte 5433 + Redis 7 + MinIO), scripts `pnpm dev:up/down/logs/reset`, init SQL (extensions). *(2026-04-23)*
- [x] **CI de base** — GitHub Actions : lint + typecheck + test sur PR, audit deps séparé, Dependabot hebdo (npm + github-actions), PR template. *(2026-04-23)*
- [x] **Pre-commit hooks** — husky 9.1.7 + lint-staged 16.2.7, Biome `check --write` sur fichiers stagés, bloque les erreurs non auto-fixables. *(2026-04-24)*

## Phase 1 — Fondations data & auth

- [x] **Package `db`** — Drizzle config + schéma initial (`tenants`, `users`, `memberships`, `sessions`, `missions` minimal, `mission_members`), migration `0000_init`, seed dev (tenant demo) idempotent. *(2026-04-24)*
- [x] **RLS** — migration `0001_rls` : rôles `app_user` (NOBYPASSRLS) + `app_admin` (BYPASSRLS), FORCE RLS sur les 6 tables, 21 policies, helper `app_current_uuid(text)`. Tests Vitest + Testcontainers couvrent isolation inter-tenants, GUC absente = 0 rows, bypass admin (11 tests verts). *(2026-04-24)*
- [x] **Package `shared-schemas`** — primitives (`Uuid`, `Email`, `NonEmptyString`, `IsoDateTime`) et enveloppes (`ErrorResponse`, `PaginationQuery/Meta/PaginatedResponse`) déclarées en double TypeBox + Zod, parité validée par tests (`expectParity`). Format registry TypeBox initialisé pour `email`/`uuid`/`date-time`. *(2026-04-27)*
- [x] **API squelette** — Fastify 5 + TypeBox, JWT access (15m, HS256) + refresh tokens opaques sha256 stockés dans `sessions` (rotation + détection de réutilisation), cookies httpOnly Secure(prod) SameSite=Lax, argon2id, plugins `withAdminTx`/`withTenantTx` (RLS via SET LOCAL ROLE + GUCs `app.current_user_id`/`app.current_tenant_id`), rate limit (100/min global, 5/min sur /auth/login), error handler global → enveloppe `ErrorResponse`, OpenAPI auto à `/docs`. Routes : `POST /auth/{login,refresh,logout}`, `GET /me`, `GET /health`. Tests intégration Testcontainers (7 verts). *(2026-04-27)*
- [x] **Package `rbac`** — CASL 6.8.0 isomorphic, subjects discriminés (`Tenant`/`User`/`Membership`/`Mission`/`MissionMember`), actions CRUD + `submit`/`close`. `defineAbilitiesFor(ctx)` couvre super_admin, no-tenant, cabinet_admin, et auditor avec règles fines `lead`/`contributor`/`observer` par mission. Câblé dans `apps/api` via `request.ability` (lazy, après `requireAuth`) + helper `app.requireAbility(action, subject)`. 12 tests rbac + 2 tests intégration api. *(2026-04-27)*
- [x] **Refactor `auth_identities`** — extraction des credentials hors de `users` pour préparer SSO (Google/Microsoft) et magic link. Nouvelle table `auth_identities (id, user_id, provider, provider_subject, secret_hash, email_at_link, last_used_at, ...)` avec enum `auth_provider ('password'|'google'|'microsoft'|'magic_link')`. Indexes uniques partiels : `(provider, provider_subject)` et `(user_id, provider)` filtrés `deleted_at is null`. Migration `0002_auth_identities` : création table → backfill (chaque `users.password_hash` → `auth_identity` provider='password') → DROP COLUMN → RLS (FORCE, policy SELECT/UPDATE self, INSERT/DELETE via app_admin). Login route Fastify lit le hash via jointure sur `auth_identities` filtrée par `provider='password'` et met à jour `last_used_at`. Tests RLS étendus (isolation owner-only sur identities, bypass admin, GUC absente = 0 rows). *(2026-04-28)*

## Phase 2 — Front & flux admin

- [x] **Front squelette** — Vue 3 + Vite 7 + PrimeVue 4 (Aura) + Tailwind v4 + vue-router + Pinia. Layout `AppShell` mobile-first 375 px (topbar + drawer hamburger < lg, sidebar permanente lg+). Vues `Home`/`Login`/`NotFound` (lazy). Login utilise `ZLoginRequest` de `shared-schemas` (form non câblé sur l'API — étape suivante). Vite proxy `/api → :3000`. Tests Vitest + @vue/test-utils + jsdom (2 verts). Override Biome pour `.vue` (Biome ne parse pas les templates). *(2026-04-27)*

  Bonus : `infra/docker-compose.yml` — MinIO écoute désormais sur 9010 à l'intérieur du container (et plus 9000), ce qui libère totalement le port 9000 pour d'autres stacks Docker locaux.
- [x] **Client API typé** — package `@myreport/api-client` : `createApiClient({ baseUrl, getAccessToken, fetch? })` exposant `auth.{login,refresh,logout}` et `me.get`. Inputs typés depuis TypeBox (`LoginRequest`...), réponses parsées avec les schémas Zod parité-testés (`ZLoginResponse`/`ZMeResponse`/`ZRefreshResponse`). Trois classes d'erreurs : `ApiError` (envelope `ErrorResponse` validée), `ApiContractError` (drift schéma front/API), `ApiNetworkError` (fetch a throw). `credentials: 'include'` pour le cookie refresh, `AbortSignal` propagé. Câblé dans `apps/web` via composable `useApiClient()` (singleton lazy) + Pinia store `auth.login()` qui appelle `/auth/login` ; `LoginView` redirige sur `/` au succès. 9 tests api-client + smoke test `LoginView` (form → store → redirect). *(2026-04-28)*
- [x] **Login + /me** — UX auth complète. `apps/web/main.ts` `await auth.bootstrap()` avant `app.mount` : tente `apiClient.ensureRefresh()` (cookie httpOnly), puis hydrate via `me.get` si OK ; sinon laisse le store vide (silencieux). `@myreport/api-client` étendu : intercepteur 401 (refresh + retry une fois, dédup via promise in-flight partagée) + `createRefreshScheduler({ refresh, marginSec })` qui décode `exp` du JWT et arme un timer (30 s avant expiry par défaut). Callbacks `onAccessTokenRotated` (re-arme le scheduler côté store) et `onSessionExpired` (toast + redirect via `markSessionExpired` qui bump un tick observé dans `App.vue`). Garde de route `meta.requiresAuth` → redirect `/login?redirect=<path>` ; auth → bounce `/login` vers `/`. Bouton « Se déconnecter » dans le footer du `AppShell` (visible authenticated only). 21 tests api-client (interceptor : happy path, refresh fails, parallel dédup, no-retry sur `/auth/login` ; scheduler : exp décodé, fire à temps, fire immédiat si déjà expiré, cancel, re-schedule, JWT malformé) + 5 tests store (`bootstrap` OK / no-session, `login` arme scheduler, `logout` cancel, `markSessionExpired`). *(2026-04-28)*
- [x] **Admin tenants** (super_admin) — `POST /tenants` + `GET /tenants` (super_admin only via CASL `create`/`read` Tenant). Crée tenant + user + `auth_identities` (argon2id) + membership `cabinet_admin` dans une seule `withAdminTx`. 409 distincts `SLUG_TAKEN` / `EMAIL_TAKEN`. Slug = DNS label (3-63 chars `[a-z0-9-]`). Front `/admin/tenants` (`requiresSuperAdmin`) avec form (slug auto-dérivé du nom, éditable) + DataTable PrimeVue `responsiveLayout="stack"`. Lien « Administration » dans `AppShell` visible si `isSuperAdmin`. **V1 : password initial saisi par super_admin et transmis hors-bande ; invitation par email/lien magique reportée à la story « Gestion users »** (Phase 2 story 5). Refactor RBAC : cabinet_admin ne peut plus `create`/`delete` Tenant (seulement `read`/`update`) — cohérent avec « tenant CRUD = super_admin ». Tests : edge cases listés *avant* code (slug/email taken sur actif vs réutilisable sur soft-deleted, 400 sur 5 variantes de payload, 401/403, GET filtre soft-deleted) — 16 nouveaux tests api + 4 parité shared-schemas + 3 router guard front. *(2026-04-28)*
- [x] **Gestion users** (cabinet_admin) — inviter auditors, rôles, révocation, plus le refactor de la création de tenant pour passer par une invitation cabinet_admin et l'adapter Resend pour la prod. Story livrée en 7 PRs (modèle invitations + package email + endpoints invitations + endpoints users/memberships + refactor /admin/tenants + UI gestion users & accept + driver Resend). *(2026-04-29)*
- [x] **Fix UX : super_admin gestion membres scopée par tenant** — bug remonté après merge : le super_admin pouvait ouvrir `/admin/users` mais l'invite échouait en `400 TENANT_ID_REQUIRED` faute de champ tenantId dans le front. Fix option B : ajout d'un bouton « Gérer les membres » par ligne sur `/admin/tenants` qui navigue vers `/admin/users?tenantId=<id>`. `AdminUsersView` lit la query (super_admin uniquement, ignorée pour cabinet_admin) et passe le `tenantId` à `users.list`, `invitations.list` et `invitations.create`. Header dynamique « Membres du cabinet — <name> » via lookup `tenants.list`. Empty state « Aucun cabinet sélectionné » + bouton vers `/admin/tenants` quand super_admin landing sans query. Mapping inline pour `TENANT_ID_REQUIRED`/`TENANT_NOT_FOUND`. AppShell : sidebar « Membres » désormais cabinet_admin only (super_admin entre via les cabinets). 2 nouveaux tests (super_admin sans query → empty state + 0 appel API ; super_admin avec query → list+invite scopés). 20/20 web verts. *(2026-04-29)*
  - [x] **PR 1 — modèle invitations** : ADR 0002 (table dédiée vs reuse `auth_identities`), schéma Drizzle `invitations` (id v7, tenant_id FK, email citext, role membership_role, token_hash bytea, expires_at, consumed_at, revoked_at, invited_by_user_id FK users nullable, soft-delete), partial unique `(tenant_id, email)` filtré sur lignes actives, migration `0003_invitations` avec RLS (FORCE, policies SELECT/INSERT/UPDATE/DELETE tenant-scoped). 8 tests RLS supplémentaires (isolation cross-tenant, GUC absente, partial unique bloque doublon actif, autorise re-invite après revoke/consume/soft-delete) — 21/21 verts. *(2026-04-29)*
  - [x] **PR 2 — package `@myreport/email`** : abstraction `EmailSender` (interface `send(Email)`), `Email` type (`to`, `subject`, `text`, `html?`, `replyTo?`), `ConsoleEmailSender` (capture in-memory + `reset()`, sink personnalisable, défaut = `console.info` payload structuré sans le body pour éviter de fuiter le token dans les logs ANSI). Factory `createEmailSender({ driver })` avec garde runtime `Unsupported email driver` pour les configs construites depuis input non-typé. `EMAIL_DRIVER=console` ajouté à `apps/api/src/config/env.ts` (zod enum) + `.env.example` (commentaire mentionne mailjet à venir). 9 tests verts (capture, ordre, reset, sink custom, défaut `console.info`, no-body-leak, omission champs optionnels, factory dispatch, factory rejette driver inconnu). README package complet. *(2026-04-29)*
  - [x] **PR 3 — endpoints invitations** : RBAC subject `Invitation` (cabinet_admin `manage` scopé tenant, super_admin via `manage all`, auditor refusé). Shared-schemas `dtos/invitations.ts` (TypeBox+Zod parité, `status` calculé `pending|expired|consumed|revoked`). 4 endpoints : `POST /invitations` (super_admin avec `tenantId` body / cabinet_admin sans, instance-level RBAC sur tenantId, génération token base64url 256-bit + sha256 stocké, partial-unique re-check côté app pour expiry, send email post-tx, `acceptUrl` retourné dans la 201), `GET /invitations` (filtre `status=pending|all`, super_admin avec `?tenantId=`, status calculé en SQL), `DELETE /invitations/:id` (revoke = `revoked_at`, distinct `INVITATION_ALREADY_USED` / `INVITATION_ALREADY_REVOKED`), `POST /invitations/:token/accept` (public, atomic consume guard contre la race, crée user+auth_identity+membership, issue access+refresh comme login). Service `invitations.ts` (token gen, hash, URL build, TTL 7d). Service `emails.ts` (template texte invitation). Plugin `email.ts` avec override pour les tests. Nouveau env `WEB_BASE_URL` (validé `.url()`). 63 tests api verts (24 nouveaux : 11 POST, 4 GET, 5 DELETE, 8 accept), 8 unit tests service, 5 nouveaux parité shared-schemas, 2 nouveaux rbac. *(2026-04-29)*
  - [x] **PR 4 — endpoints users/memberships** : Migration `0004_memberships_soft_delete` (ajoute `deleted_at`, partial unique `(tenant,user) WHERE deleted_at IS NULL`, index sur `deleted_at`) — préserve l'historique d'appartenance et autorise le re-add après retrait. Schéma Drizzle mis à jour. Shared-schemas `dtos/users.ts` (`UserListItem`, `UpdateMembershipRequest/Response`) avec parité TypeBox+Zod. 3 endpoints : `GET /users` (cabinet_admin scopé tenant via RLS, super_admin avec `?tenantId=` optionnel), `PATCH /memberships/:id` (idempotent si role inchangé, garde `LAST_ADMIN` sur la rétrogradation du dernier cabinet_admin actif), `DELETE /memberships/:id` (soft-delete `deleted_at`, garde `LAST_ADMIN`, **révoque les sessions actives** `(user_id, tenant_id)` via `withAdminTx` pour invalider les JWT en cours). 17 nouveaux tests intégration (GET ×6, PATCH ×7, DELETE ×4) + 1 test RLS partial unique re-add + 3 parité shared-schemas. **80/80 api verts**, 22/22 db verts. Auditor explicitement refusé sur GET via le check `manage:User`. *(2026-04-29)*
  - [x] **PR 5 — refactor `/admin/tenants`** : `POST /tenants` ne crée plus user/auth_identity/membership directement. Body simplifié `{ name, slug, adminEmail }` (plus de `firstAdmin.password`/`displayName`). Crée tenant + invitation `role='cabinet_admin'` atomique dans `withAdminTx`, envoie l'email d'invitation post-tx via `app.emailSender`, retourne `{ tenant, invitation: { id, email, role, expiresAt, acceptUrl } }`. Refus 409 `EMAIL_TAKEN` upstream si l'email mappe à un user global existant (le multi-tenant user reste différé). 500 `EMAIL_DELIVERY_FAILED` si l'envoi échoue après commit (DB cohérente, l'admin peut révoquer/réémettre). Front `AdminTenantsView` : retire les champs `displayName`/`password`, garde `name`/`slug`/`adminEmail`, affiche un panneau persistant après création avec le `acceptUrl` copiable (utile en dev/console driver). `GET /tenants` : `membershipCount` filtre désormais les memberships `deleted_at IS NULL` pour rester cohérent avec PR 4. Tests : tenants.test.ts entièrement réécrit (10 tests : happy-path bout-en-bout `créer → accepter → login`, 401/403, 4 variantes 400, rejet payload legacy `firstAdmin`, `SLUG_TAKEN`, `EMAIL_TAKEN`, slug/email réutilisable après soft-delete) + happy path vérifie l'email envoyé via `ConsoleEmailSender.sent`. Parité shared-schemas mise à jour. **80/80 api verts**, 19 shared-schemas, 11 web. *(2026-04-29)*
  - [x] **PR 6 — front** : `@myreport/api-client` étendu (`users.list`, `memberships.update/remove`, `invitations.create/list/revoke/accept`). Vue `AdminUsersView` (`/admin/users`, accessible super_admin OU cabinet_admin via meta `requiresAdmin`) : form d'invitation (email + select rôle), DataTable membres actifs avec Select rôle inline + bouton actions (`Retirer` / `Quitter le cabinet` selon self), DataTable invitations en attente avec révocation. Modale de confirmation **dédiée** pour les self-actions : self-demotion → confirm + sync `/me` + redirect home, self-removal → confirm + `auth.forceLocalLogout()` + redirect `/login` (ne rappelle PAS `/auth/logout` puisque l'API a déjà révoqué les sessions). `LAST_ADMIN` 409 affiché inline dans la modale plutôt qu'en toast pour rester visible. Vue publique `AcceptInvitationView` (`/invitations/accept?token=...`) : form mdp+displayName, branche les codes terminaux (404/409/410) sur un état fatal qui masque le form et oriente vers `/login`. AppShell sidebar : « Cabinets » (super_admin) + « Membres » (cabinet_admin OU super_admin). Store auth : `forceLocalLogout()` (cleanup sans appeler /auth/logout) et `completeAcceptedInvitation()`. Tests web : 7 nouveaux (4 AdminUsersView : list+invite, LAST_ADMIN inline, self-removal logout ; 3 AcceptInvitationView : token manquant, success hydrate store, code terminal hide form). 18/18 web verts. *(2026-04-29)*
  - [x] **PR 7 — adapter Resend** : ADR 0003 documente le choix Resend (vs Mailjet/Postmark/SES) sur les critères EU hosting + free tier 3000/mois + simplicité API REST + future-proof via `react-email`. Driver `ResendEmailSender` (raw `fetch` POST `https://api.resend.com/emails`, Bearer auth, override `fetch` + `endpoint` pour tests, mapping `Email` → payload Resend, jamais de leak de la clé API dans les erreurs). Type `EmailAddress` ajouté à `sender.ts`, `from` configuré au driver à la construction (pas par message → templates portables). `ConsoleEmailSender` accepte `from` optionnel pour parité, default sink l'affiche en `Name <addr>`. `EmailConfig` discriminé `console | resend`. Env `EMAIL_FROM_ADDRESS`/`EMAIL_FROM_NAME`/`RESEND_API_KEY` optionnels au schéma + `superRefine` exige les requis quand `driver=resend` (erreur claire ciblant le champ manquant). Plugin `email.ts` traduit le flat env en `EmailConfig`. README package étendu (les deux drivers + recette tests + sandbox via clé test). 8 nouveaux tests resend (mock fetch : payload conforme, omission `html`/`reply_to`, format `from` sans/avec name, erreur API JSON+texte+network, override endpoint, no-leak api key) + 1 factory dispatch resend. **18/18 email**, 80/80 api inchangés. *(2026-04-29)*

## Phase 3 — Cœur métier

- [~] **Templates questionnaires** — éditeur Monaco (JSON validé par méta-schema TypeBox), versionnage immuable. Story découpée en 4 PRs (schema + DB + API + front), et éventuellement une 5e si on extrait le renderer.
  - [x] **PR 1 — ADR 0004 + package `@myreport/questionnaire-schema`** : DSL TypeBox (union discriminée `kind` : `section`, `text`, `longText`, `number`, `boolean`, `singleChoice`, `multiChoice`, `scale`, `date`, `attachment`, `repeater`), root `version: 1` réservé pour évolutions DSL futures. Sections non imbriquables au niveau du type (`TSectionContent = TLeafQuestion | TRepeater`) ; repeater accepte uniquement des leaves (pas de repeater dans repeater, pas de section dans repeater). `validateQuestionnaireSchema(input)` en deux passes : shape via TypeBox `Value.Check`/`Value.Errors` (code `SHAPE`) puis règles structurelles (codes `DUPLICATE_ID`, `INVALID_RANGE`, `INVALID_PATTERN`, `DUPLICATE_OPTION_VALUE`, `INVALID_INTEGER_BOUND`, `INVALID_MIME`, `INVALID_DATE`). IDs unifiés en un seul namespace global (sections + leaves + repeater children) — simpler pour le renderer. Paths d'erreurs au format `sections[0].questions[1].options[2]` (conversion JSON Pointer → friendly). `toJsonSchema()` exporte un deep-copy JSON-Schema-compatible pour Monaco (mutation-safe). `walkQuestionnaire(schema, visit)` exposé pour les futurs consumers (renderer, report-engine, IA cache). 53 tests (12 happy paths un par kind + multi-section + descriptions ; 8 top-level shape ; 4 id-uniqueness ; 3 text/longText ; 3 number ; 6 choice ; 3 scale ; 3 date ; 2 attachment ; 3 repeater ; 3 path formatting ; 3 JSON Schema export ; 1 walk). Edge cases listés *avant* code, chacun un test. *(2026-05-12)*
  - [x] **PR 2 — DB : `questionnaire_templates` + `questionnaire_template_versions` + RLS** : Migration `0005_questionnaire_templates` (1 enum `questionnaire_template_version_status` + 2 tables UUID v7 + FK cascade vers `tenants`, FK cascade `versions → templates`, FK `templates.current_version_id → versions.id` set null, FK `versions.published_by_user_id → users.id` set null). Soft-delete sur `templates` uniquement (versions immuables) avec partial unique `(tenant_id, slug) WHERE deleted_at IS NULL`. Unique `(template_id, version)` + index `(template_id, status)`. CHECK constraints : `version >= 1` et `published_at` ↔ `status` cohérents (`draft = NULL`, `published`/`archived` = NOT NULL). RLS : 4 policies par table (SELECT/INSERT/UPDATE/DELETE) scopées sur `app_current_uuid('app.current_tenant_id')`, GRANT app_user + app_admin, ENABLE+FORCE. **Trigger d'immutabilité** `qtv_immutability` BEFORE UPDATE OR DELETE avec `WHEN (current_user = 'app_user')` — ne fire que pour cabinet_admin via l'API, bypass naturel pour app_admin (super_admin, purges) et pour les cascades RI (qui s'exécutent en tant que table owner). Logique : draft = mutable + DELETE OK ; published = transition vers archived uniquement + champs immuables (schema/version/template_id/tenant_id/published_at/published_by_user_id), DELETE rejeté ; archived = gelée totalement. Toutes les rejections en SQLSTATE 23514 (check_violation). 27 tests Testcontainers (5 RLS isolation + 3 slug partial unique + 3 version monotonicity + 2 published_at invariant + 11 trigger transitions + 2 cascade + 1 bypass admin) — **48/48 db verts**. Edge cases listés *avant* code. *(2026-05-12)*
  - [~] **PR 3 — API endpoints (CRUD templates + versions + publish/archive)** : découpé en deux pour limiter le scope par PR.
    - [x] **PR 3a — Templates CRUD** : RBAC subject `Template` (cabinet_admin `manage` scopé tenant, super_admin via `manage all`, auditor refusé). Shared-schemas `dtos/questionnaire-templates.ts` (parité TypeBox+Zod sur slug DNS-label, CreateRequest avec `tenantId` optionnel super_admin-only, UpdateRequest n'expose ni slug — immuable — ni currentVersionId — géré par publish/promote en PR 3b, Response avec `description`/`currentVersionId` nullable). 5 endpoints (`POST /templates`, `GET /templates`, `GET /:id`, `PATCH /:id`, `DELETE /:id`) suivant le pattern invitations : cabinet_admin via `withTenantTx` (RLS scope automatique), super_admin via `withAdminTx` + `?tenantId=` query (GET list) / body field (POST). Contrats stricts : 400 `TENANT_ID_REQUIRED` pour super_admin sans tenantId, 400 `TENANT_ID_FORBIDDEN` si cabinet_admin tente de spécifier tenantId, 409 `SLUG_TAKEN` (pre-check + partial unique index back-stop), 404 `TENANT_NOT_FOUND` sur tenantId inconnu, 204 sur DELETE (soft-delete). Re-check RBAC d'instance après resolution du tenant pour bloquer un token périmé. PATCH no-op (body = `{}` ou unknown fields stripés par Fastify `removeAdditional: true`) → renvoie la row inchangée plutôt que de faire planter Drizzle sur SET vide. 28 tests d'intégration Testcontainers (11 POST, 4 GET, 4 GET:id, 5 PATCH, 4 DELETE) couvrant happy paths, RLS cross-tenant, slug reusable après soft-delete, super_admin + auditor + unauthenticated. **108/108 api verts**, 24/24 shared-schemas, 16/16 rbac. *(2026-05-12)*
    - [ ] **PR 3b — Versions + lifecycle (publish/archive)** : 7 endpoints autour de `questionnaire_template_versions`. Validation DSL body via `@myreport/questionnaire-schema`, auto-pick `max(version) + 1` en tx, mapping SQLSTATE 23514 → 409 pour les transitions interdites par le trigger d'immutabilité.
  - [ ] **PR 4 — front éditeur Monaco**.
- [ ] **Missions** — CRUD, assignation d'auditors, cycle `draft → in_progress → submitted → closed`.
- [ ] **Remplissage** — formulaire dynamique généré depuis le `schema` JSONB du template version, validation type-aware, pièces jointes vers S3.
- [ ] **Invitation auditee** — email + lien magique scopé mission, écran de remplissage allégé.

## Phase 4 — Rapports & IA

- [ ] **Package `report-engine`** — génération docx via docxtemplater, placeholders + boucles, résolution branding tenant.
- [ ] **Worker** (app) — BullMQ, job `generate-report`, stockage sortie sur S3, SSE vers le front.
- [ ] **Package `ai`** — abstraction `AIProvider`, implémentation Anthropic par défaut, OpenAI fallback, cache `(mission_id, question_id, input_hash)`.
- [ ] **UI réécriture** — bouton « Reformuler » sur champs texte long, toggle global par cabinet.
- [ ] **Mode BYOK** — écran config clé API, chiffrement libsodium, rotation clé maître documentée.
- [ ] **Templates rapport par cabinet** — upload `.docx`, validation placeholders, manifest JSON des variables.

## Phase 5 — Polish MVP

- [ ] **Export markdown → PDF** (optionnel, si demandé).
- [ ] **Monitoring rapports IA** — `ai_usage` dashboard cabinet + super-admin, quotas.
- [ ] **E2E Playwright** — parcours : login, créer mission, remplir, générer rapport.
- [ ] **Runbook déploiement** — docs/runbook/, doc Hetzner + Scaleway ou équivalent.

## Reportés / V2+

- [ ] Offline mode (sync SQLite client, CRDT/LWW, résolution conflits).
- [ ] Signature électronique (DocuSign / Yousign).
- [ ] API publique pour cabinets.
- [ ] Marketplace de templates.
- [ ] Mobile natif (si vraiment demandé).

---

## Décisions ouvertes

- Hébergement prod (Hetzner probable, Postgres managé vs auto).
- Emails transactionnels (Postmark / Resend / SMTP).
- Observabilité (Pino + Loki/Axiom/Better Stack).

## Dette technique déclarée

- **Dette dépendances (snapshot 2026-05-12).** Inventaire pris au moment du chore deps PR #40. À retraiter à intervalle régulier (début de sprint ou avant chaque PR majeure d'app).
  - **Majors en attente, une PR par paquet avec ADR si breaking** :
    - `jsdom 27 → 29` (dev-only, breaking modeste a priori).
    - `lint-staged 16 → 17` (outil dev, breaking probablement cosmétique).
    - `vite 7 → 8` (risque vrai sur le build front).
    - `vue-router 4 → 5` (le plus risqué, breaking docs à lire).
  - **`esbuild` moderate dev-only via `drizzle-kit > @esbuild-kit/esm-loader > @esbuild-kit/core-utils > esbuild`.** Le chain `@esbuild-kit/*` est déprécié upstream. Fix réel = bump majeur de `drizzle-kit`. Override `esbuild >= 0.25.0` envisageable mais risque que drizzle-kit ne tourne plus.
  - **Bumps possibles dès franchissement 90j** (à recheck) : `@vue/test-utils 2.4.10`, `postgres 3.4.9`, `primevue 4.5.5`, `vue-tsc 3.2.8`, `@tailwindcss/vite 4.3.0`, `tailwindcss 4.3.0`, `uuidv7 1.2.1`, `zod 4.4.3`, `@sinclair/typebox 0.34.49`, `@vue/tsconfig 0.9.1`. À regrouper dans une chore quand 3+ deps franchissent ensemble la barre.
  - **Versions installées en zone < 90j** (rétrospectivement hors règle ; ne pas downgrader, surveiller) : `turbo 2.9.6` (90j-safe = 2.8.7), `vitest 4.1.5` (= 4.0.18), `@types/node 25.6.0` (= 22.x), `lint-staged 16.4.0` (= 16.2.7). Vérifier au prochain check `npm view <pkg> time` que les versions actuelles ont franchi 90j depuis leur publication, et appliquer la règle au prochain bump.
- **Console : 401 sur `/auth/refresh` au boot pour les visiteurs non loggués.** Comportement fonctionnel correct (le `try/catch` dans `auth.bootstrap()` avale silencieusement et l'app redirige proprement vers `/login`), mais le navigateur logue la status 401 dans la console — bruit cosmétique. Fix pressenti : flag non-sensible `localStorage['myreport.had-session']` posé au login / retiré au logout, et `bootstrap()` n'appelle `ensureRefresh()` que si le flag est présent. Tradeoff : un user qui purge son localStorage manuellement devra se re-logguer même si son cookie refresh est encore valide. À embarquer dans Phase 5 (polish MVP) ou dans une chore PR dédiée.
