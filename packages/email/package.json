{
  "name": "@myreport/email",
  "version": "0.0.0",
  "private": true,
  "description": "Email sender abstraction for myReport (transactional outbound mail)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "vitest": "4.1.6"
  }
}
